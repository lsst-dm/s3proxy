### New features

- Filter S3 objects by MIME type. Only objects whose guessed MIME type is in the configured allow list (`S3PROXY_ACCEPT_MIMETYPES` plus `S3PROXY_ALSO_ALLOW_MIMETYPES`, minus `S3PROXY_DISALLOW_MIMETYPES`) are served; other requests are rejected with HTTP 415.
